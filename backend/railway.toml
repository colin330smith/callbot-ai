[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
startCommand = "uvicorn main_production:app --host 0.0.0.0 --port ${PORT:-8080}"
restartPolicyType = "on-failure"
